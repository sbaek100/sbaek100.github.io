---
layout: post
title: "(3주차) MySQL 기초 3-1: 데이터 필터링 (아주 쉽게 따라하기)"
date: 2026-03-04 03:01:00 +0900
categories:
  - 강의
  - 데이터베이스보안
tags:
  - mysql
  - where
  - filtering
  - beginner
---

# 데이터 필터링 (3-1)

## 자료형 요약표

|자료형|저장 내용|예시 데이터|대표 사용|
|---|---|---|---|
|`INT`|정수|1, 100, -5|번호, 점수, 수량|
|`VARCHAR(n)`|가변 문자열|'김철수', 'abc@gmail.com'|이름, 이메일, 도시|
|`TEXT`|긴 문자열|'오늘 수업은...(긴 글)'|본문, 설명, 후기|
|`DECIMAL(p,s)`|정확한 소수|29900.00, 0.30|가격, 정산 금액|
|`DATE`|날짜|2025-03-16|생년월일, 만료일|
|`DATETIME`|날짜+시간|2025-03-16 09:30:00|가입일시, 로그|

다음은 **실습(테이블 생성 → 데이터 삽입 → 조회)** 으로 넘어갈까요?

---

## 1. 준비

```sql
CREATE DATABASE filter_db;
USE filter_db;

CREATE TABLE students (
  id INT,
  name VARCHAR(20),
  grade INT,
  city VARCHAR(20),
  score INT
);
```

```sql
INSERT INTO students VALUES (1, '철수', 1, '서울', 85);
INSERT INTO students VALUES (2, '영희', 2, '부산', 92);
INSERT INTO students VALUES (3, '민수', 1, '서울', 74);
INSERT INTO students VALUES (4, '지은', 3, '대전', 88);
INSERT INTO students VALUES (5, '준호', 2, '부산', 67);
```

---

## 2. WHERE 기본 실습

```sql
SELECT * FROM students WHERE score >= 80;
SELECT * FROM students WHERE city = '서울';
SELECT * FROM students WHERE grade = 2;
```

---

## 3. AND / OR 실습

```sql
SELECT * FROM students
WHERE city = '부산' AND score >= 70;

SELECT * FROM students
WHERE city = '서울' OR city = '대전';
```

---
