# --- 기존 ?�정 ---
# Bundler cache
.bundle
vendor
Gemfile.lock

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# NPM dependencies
node_modules
package-lock.json

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
_sass/vendors
assets/js/dist

# --- 보안 추�? ?�정 (중요) ---
.env
*.pem
*.key
id_rsa
id_rsa.pub
secrets.json`r`n`r`n# Local Obsidian workspace/config should not be published`r`n.obsidian/`r`n
# Local Obsidian workspace/config should not be published
.obsidian/
bfg.jar
*.jar
.claude/settings.local.json

# 강의 원본(대용량 바이너리) — 사이트 자산 아님
assets/img/posts/*.pptx
